apiVersion: batch/v1
kind: Job
metadata:
  generateName: smithy-busybox-build
spec:
  ttlSecondsAfterFinished: 60
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: smithy
        image: ghcr.io/rapidfort/smithy:latest
        args:
          - --context=https://github.com/docker-library/busybox.git
          - --context-sub-path=latest/glibc
          - --dockerfile=Dockerfile
          - --destination=busybox
          - --no-push
          - -v
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop: [ALL]
            add: [SETUID, SETGID]