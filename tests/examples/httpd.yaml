apiVersion: batch/v1
kind: Job
metadata:
  generateName: smithy-httpd-build
spec:
  ttlSecondsAfterFinished: 60
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: smithy
        image: ghcr.io/rapidfort/smithy:latest
        args:
          - --context=https://github.com/docker-library/httpd.git
          - --context-sub-path=2.4/alpine
          - --dockerfile=Dockerfile
          - --destination=10.228.96.114:5000/my-httpd
          - --no-push
          - -v
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop: [ALL]
            add: [SETUID, SETGID]