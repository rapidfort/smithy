apiVersion: batch/v1
kind: Job
metadata:
  generateName: smithy-ubuntu-build
spec:
  ttlSecondsAfterFinished: 60
  template:
    spec:
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
      - name: smithy
        image: ghcr.io/rapidfort/smithy:latest
        args:
          - --context=https://github.com/dockerfile/ubuntu.git
          - --dockerfile=Dockerfile
          - --destination=10.228.96.114:5000/dockerfile/ubuntu
          - -v
          - --insecure-registry=10.228.96.114:5000
        securityContext:
          allowPrivilegeEscalation: true
          capabilities:
            drop: [ALL]
            add: [SETUID, SETGID]